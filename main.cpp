#include <iostream>                         // include directive. Make other files availble to us.
                                            // iostream is a standard library for IO

using namespace std;                        // similar to a static import in java

int main() {                                // standard signature for a main function
    cout << "Hello, World!" << endl;        // cout = character output stream, << insertion operator, endl = \n
    return 0;                               // return type  = exit code
}